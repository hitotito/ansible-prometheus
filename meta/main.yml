---
galaxy_info:
  author: Hitoshi Okonogi
  description: role to install prometheus
  license: MIT
  min_ansible_version: 2.2
  platforms:
    - name: Debian
      versions:
        - jessie
        - stretch
  galaxy_tags:
    - monitoring
    - nodeexporter
    - prometheus
    - system

dependencies:
  - role: hitotito.custom-facts
    custom_facts_name: hitotito_prometheus
    custom_facts_file: prometheus.fact
